<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>2FA Status</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body {
            font-family: 'Inter', sans-serif;
        }
    </style>
</head>
<body class="bg-gray-50 flex flex-col min-h-screen">
    <!-- Header -->
    <header class="bg-white shadow-sm">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-4">
            <h1 class="text-2xl font-bold height-16 text-gray-900">Account Security</h1>
        </div>
    </header>

    <!-- Main Content -->
    <main class="flex-grow flex items-center justify-center py-12">
        <div class="max-w-md mx-auto bg-white rounded-lg shadow-md p-6 text-center">
            <% if (status === 'success') { %>
                <h2 class="text-2xl font-semibold text-green-600 mb-4">2FA Disabled Successfully</h2>
                <p class="text-gray-600 mb-6">Two-factor authentication has been successfully disabled for your account.</p>
                <p class="text-gray-500 mb-8">You can re-enable it anytime from your account settings for enhanced security.</p>
                <a href="<%= link %>" class="inline-block px-6 py-3 bg-green-600 text-white font-semibold rounded-md hover:bg-green-700 transition duration-300">Go to <%= link === "/customer" ? "Customer" : "Admin" %> Dashboard</a>
            <% } else { %>
                <h2 class="text-2xl font-semibold text-red-600 mb-4">2FA Disable Failed</h2>
                <p class="text-gray-600 mb-6">There was an issue disabling two-factor authentication. Please try again.</p>
                <p class="text-gray-500 mb-8">If the problem persists, contact support for assistance.</p>
              
            <% } %>
        </div>
    </main>

    <!-- Footer -->
    <footer class="bg-white text-gray-500 py-4">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
            <p>&copy; <%= new Date().getFullYear() %> Your Company. All rights reserved.</p>
        </div>
    </footer>
</body>
</html>